{
  "name": "a11y-page-checker-workspace",
  "private": true,
  "packageManager": "pnpm@11.21.0",
  "scripts": {
    "build": "pnpm -r --if-present run build",
    "test": "pnpm -r --if-present run test"
  },
  "engines": {
    "node": ">=22.13"
  }
}
